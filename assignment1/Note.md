```python
np.sum() # axis = 1:按行求和；axis = 0:按列求和
np.sqrt()
np.square()
np.argmax/np.argmin()
np.argsort() # 返回排序后的索引 倒序排序：np.argsort(arr)[::-1]
np.array()
vals, counts = np.unique(arr, return_counts = True)
```